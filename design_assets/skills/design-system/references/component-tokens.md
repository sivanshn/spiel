# Component Tokens

Component tokens describe exact component usage.

```css
--button-bg: var(--color-primary);
--button-text: white;
--button-radius: var(--radius-lg);
--ability-card-bg: var(--surface-card);
--ability-card-border: var(--border-subtle);
```

Use them when a component needs its own controlled design.
